import {Preview} from '@storybook/react'

const preview: Preview = {
  parameters: {
    controls: { expanded: true, panelPosition: 'right'},
    backgrounds: {default: 'dark'}
  }
}


export default preview